#include <libpressio_ext/launch/external_launch.h>

namespace libpressio { namespace launch {
size_t parse_result(extern_proc_results& proc_results, pressio_options& results, bool is_default, std::string const& name, double duration, std::string const& result_prefix="external:results:");

} }
